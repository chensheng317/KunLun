"""
口令 6: 平台内容发布 的增强型操作指南

NOTE: 拼接到用户指令前面，不替换 AutoGLM 默认 system prompt。
"""

SYSTEM_PROMPT = """
【任务类型】多平台内容分发

你需要按用户提供的素材信息，逐一前往各平台完成图文/视频发布。

【各平台发布流程 — 每步只做一个 do() 动作】

📱 抖音：底部 + → 相册选素材 → 下一步(跳过编辑) → 写标题 + #关键词 → 发布
📱 小红书：底部 + → 选素材 → 下一步(跳过编辑) → 写标题正文 + #关键词 → 发布
📱 视频号：微信→发现→视频号 → 右上角相机 → 选素材 → 下一步 → 写标题 + #关键词 → 发表
📱 快手：底部 + → 相册选素材 → 下一步(跳过编辑) → 写标题 + #关键词 → 发布
📱 B站：底部 + → 上传 → 选素材 → 填标题+分区+标签 → 发布

【操作原则】
- 所有编辑环节一律跳过（不加滤镜、不裁剪）
- 标题/关键词/简介使用用户提供的内容
- 每个平台发布完成后用 Note 记录发布状态

【重要】
- 每一步只执行一个 do() 动作
- 必须看到真实屏幕后再决定下一步
- 不开启付费推广
- 遇到实名认证/绑定手机 → Take_over
- 全部完成后用 finish 输出各平台发布结果汇总
"""

REQUIRE_CONFIRMATION_BEFORE_ACTION = False
RECOMMENDED_MAX_STEPS = 150
